// ==== Configuration GitHub (à adapter) ====
window.APP_CONFIG = {
  owner: "mathislef34",            // ← ton nom d'utilisateur/org GitHub
  repo: "Appartements",     // ← nom du repo
  branch: "main",            // ← branche Pages (ex: main)
};

