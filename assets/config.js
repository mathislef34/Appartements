// ==== Configuration GitHub (à adapter) ====
window.APP_CONFIG = {
  owner: "owner",            // ← ton nom d'utilisateur/org GitHub
  repo: "apartment-map",     // ← nom du repo
  branch: "main",            // ← branche Pages (ex: main)
};
